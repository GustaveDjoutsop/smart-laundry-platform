# Smart Laundry Dashboard — Local Development
# Copy to .env.local, then run: doppler run --project smart-laundry-dashboard --config dev -- npm run dev

# BFF via API Gateway
NEXT_PUBLIC_BFF_URL=http://localhost:8080/reports/api
NEXT_PUBLIC_PAYMENTS_URL=http://localhost:8080/payments

# Auth0 v4 (injected by Doppler — do NOT commit real values)
AUTH0_DOMAIN=dev-iuo6si32jobgnmod.eu.auth0.com
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_SECRET=
APP_BASE_URL=http://localhost:3000
